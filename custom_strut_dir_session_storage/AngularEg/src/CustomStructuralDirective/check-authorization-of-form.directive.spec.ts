import { CheckAuthorizationOfFormDirective } from './check-authorization-of-form.directive';

describe('CheckAuthorizationOfFormDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckAuthorizationOfFormDirective();
    expect(directive).toBeTruthy();
  });
});
